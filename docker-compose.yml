services:
  handler:
    build: .
    image: handler-one-discord-bot:latest
    container_name: handler-bot
    restart: unless-stopped
    env_file:
      - .env
    # No ports exposed — the bot dials out to Discord via WebSocket.
    # Uncomment if you mount a future MCP server container here.
    # depends_on:
    #   - restaurant-mcp
